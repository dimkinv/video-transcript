# Agent Instructions (React)

- Follow SOLID principles and keep clear separation of concerns.
- Use Vite with TypeScript and keep the file set minimal.
- Prefer a small App.tsx and main.tsx bootstrap only.
- Keep components focused and avoid mixing UI with data fetching or state orchestration.
